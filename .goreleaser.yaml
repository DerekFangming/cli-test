project_name: sp-cli
builds:
  - goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
dockers:
- image_templates:
    - synfm/cli-test:{{.Tag}}
  dockerfile: Dockerfile
  build_flag_templates:
  - "--platform=linux/arm64/v8"